package lcmtypes;

struct status_t {
    int32_t  pid;
    bool     success;        // true = subscriber kept up, false = timed out
    int32_t  retry_number;   // which retry attempt this refers to; -1 == “stop”
}
